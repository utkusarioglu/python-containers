name: music
channels:
  - defaults
dependencies:
  - anaconda::jupyter
  - conda-forge::music21
  - conda-forge::matplotlib
# These may be enabled later, depending on need
# - anaconda::numpy
# - anaconda::scipy
# - cogsci::pygame
