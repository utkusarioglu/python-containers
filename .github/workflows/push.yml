name: Push

on:
  push

jobs:
  push:
    uses: ./.github/workflows/build.wfc.yml
    with:
      image_tag: 3.13-ubuntu-test-14
      base_tag: noble-test-8
    secrets:
      dockerhub_username: ${{ secrets.DOCKERHUB_USERNAME }}
      dockerhub_token: ${{ secrets.DOCKERHUB_TOKEN }}
